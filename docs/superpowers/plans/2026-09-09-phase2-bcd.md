# Phase 2 B+C+D 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 딥링크(B) + 가격 추세 통계(C) + 항공사 공식 이벤트 크롤러(D) 를 추가해 TripPick을 가격 알림 이상의 여행 정보 도구로 확장한다.

**Architecture:** EventAdapter 패턴(sources/events/)으로 항공사별 HTML 파서를 격리. 딥링크는 순수 유틸 함수. 가격 통계는 on-the-fly SQL 집계.

**Tech Stack:** Python(BeautifulSoup4, httpx), FastAPI, SQLAlchemy, Alembic, Next.js(React 19, TanStack Query), Tailwind CSS

**Spec:** `docs/superpowers/specs/2026-09-09-phase2-bcd-design.md`

## Global Constraints

- Python 3.12+, uv 패키지 매니저 (`uv run pytest`, `uv run alembic`)
- CLAUDE.md Rule 9: 봇 차단 우회 금지. 크롤러는 반드시 `sources/policy.py` 게이트 통과.
- CLAUDE.md Rule 4: DB 시각 전부 UTC timestamptz.
- CLAUDE.md Rule 5: 가격은 원화 정수(int) 저장. float 금지.
- CLAUDE.md Rule 6: 도메인 로직은 슬랙을 모른다. 알림은 `NotificationMessage`를 만들고 렌더러에서만 채널 변환.
- 어댑터 테스트는 실제 HTTP 호출 없이 fixture HTML 파일로만.
- DB: Postgres (`DATABASE_URL` env). SQLite 금지.
- 커밋: 한 커밋 = 한 논리 단위. 백엔드·프론트 섞지 않음.
- 완료 선언 전 `docker compose up` + 실제 엔드포인트 호출로 동작 확인 필수.

---

## 파일 맵

**신규 생성**
```
backend/app/utils/__init__.py
backend/app/utils/deep_links.py
backend/app/sources/events/__init__.py
backend/app/sources/events/base.py
backend/app/sources/events/airbusan.py
backend/app/sources/events/tway.py
backend/app/sources/events/jejuair.py
backend/app/sources/events/koreanair.py
backend/app/sources/events/flightdeal.py
backend/app/models/event.py
backend/app/schemas/event.py
backend/app/api/routes/events.py
backend/alembic/versions/<hash>_add_airline_events.py
backend/tests/fixtures/events/airbusan.html
backend/tests/fixtures/events/tway.html
backend/tests/fixtures/events/jejuair.html
backend/tests/fixtures/events/koreanair.html
backend/tests/fixtures/events/flightdeal.html
backend/tests/test_deep_links.py
backend/tests/sources/events/test_parsers.py
backend/tests/test_event_worker.py
backend/tests/test_stats_endpoint.py
web/hooks/useEvents.ts
web/components/watch-detail/PriceHistoryChart.tsx
web/app/events/page.tsx
web/app/events/components/EventCard.tsx
```

**수정**
```
backend/app/notify/base.py       (NotificationMessage에 deep_links 필드)
backend/app/notify/slack.py      (딥링크 버튼 + 이벤트 알림 포맷)
backend/app/schemas/watch.py     (OfferOut에 deep_links, WatchStats 추가)
backend/app/api/routes/watches.py (offers에 deep_links 주입, stats 엔드포인트)
backend/app/sources/policy.py   (이벤트 크롤러 허용 도메인 추가)
backend/app/models/__init__.py   (AirlineEvent import)
backend/app/main.py              (events_router 등록)
backend/app/worker.py            (event_tick 추가)
web/lib/types.ts                 (OfferOut.deep_links, WatchStats, EventOut)
web/components/watch-detail/OfferList.tsx  (딥링크 버튼)
web/components/layout/Nav.tsx    (이벤트 메뉴)
web/app/watches/[id]/page.tsx    (PriceHistoryChart 추가)
web/hooks/useWatches.ts          (useWatchStats 추가)
```

---

### Task 1: B-백엔드 — 딥링크 유틸 + API 오퍼 주입 + 슬랙 버튼 (TDD)

**Files:**
- Create: `backend/app/utils/__init__.py`
- Create: `backend/app/utils/deep_links.py`
- Modify: `backend/app/notify/base.py:23-32` (NotificationMessage에 deep_links 필드)
- Modify: `backend/app/notify/slack.py:15-55` (딥링크 버튼 렌더링)
- Modify: `backend/app/schemas/watch.py:167-184` (OfferOut에 deep_links 필드)
- Modify: `backend/app/api/routes/watches.py:130-159` (offers 반환 시 deep_links 주입)
- Create: `backend/tests/test_deep_links.py`

**Interfaces:**
- Produces: `build_deep_links(origin, destination, dep_date, ret_date) -> dict[str, str]`
- Produces: `OfferOut.deep_links: dict[str, str] | None`
- Produces: `NotificationMessage.deep_links: dict[str, str] | None`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# backend/tests/test_deep_links.py
import pytest
from app.utils.deep_links import build_deep_links

def test_roundtrip_returns_four_keys():
    links = build_deep_links("ICN", "NRT", "2026-10-01", "2026-10-05")
    assert set(links.keys()) == {"skyscanner", "google", "naver", "tripdotcom"}

def test_oneway_returns_four_keys():
    links = build_deep_links("ICN", "NRT", "2026-10-01", None)
    assert set(links.keys()) == {"skyscanner", "google", "naver", "tripdotcom"}

def test_skyscanner_url_format_roundtrip():
    links = build_deep_links("ICN", "NRT", "2026-10-01", "2026-10-05")
    assert "ICN" in links["skyscanner"]
    assert "NRT" in links["skyscanner"]
    assert "261001" in links["skyscanner"]  # YYMMDD
    assert "261005" in links["skyscanner"]

def test_skyscanner_url_format_oneway():
    links = build_deep_links("ICN", "NRT", "2026-10-01", None)
    # 편도: 귀국 날짜 없이 URL이 만들어져야 함
    assert "skyscanner" in links["skyscanner"]
    assert "NRT" in links["skyscanner"]

def test_naver_oneway_no_return_param():
    links = build_deep_links("ICN", "NRT", "2026-10-01", None)
    assert "returnDate" not in links["naver"]

def test_naver_roundtrip_has_return_param():
    links = build_deep_links("ICN", "NRT", "2026-10-01", "2026-10-05")
    assert "returnDate" in links["naver"]

def test_tripdotcom_oneway():
    links = build_deep_links("ICN", "NRT", "2026-10-01", None)
    assert "OW" in links["tripdotcom"]

def test_tripdotcom_roundtrip():
    links = build_deep_links("ICN", "NRT", "2026-10-01", "2026-10-05")
    assert "RT" in links["tripdotcom"]
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
cd backend && uv run pytest tests/test_deep_links.py -v
```

Expected: FAIL with `ModuleNotFoundError` 또는 `ImportError`

- [ ] **Step 3: 유틸 구현**

`backend/app/utils/__init__.py` (빈 파일)

`backend/app/utils/deep_links.py`:
```python
"""각 OTA 사이트에 사전 입력된 검색 URL을 생성하는 순수 함수."""

from __future__ import annotations


def build_deep_links(
    origin: str,
    destination: str,
    dep_date: str,          # "YYYY-MM-DD"
    ret_date: str | None,   # None이면 편도
) -> dict[str, str]:
    dep_yy = dep_date[2:4]
    dep_mm = dep_date[5:7]
    dep_dd = dep_date[8:10]
    dep_yymmdd = f"{dep_yy}{dep_mm}{dep_dd}"
    dep_nodash = dep_date.replace("-", "")

    if ret_date:
        ret_yy = ret_date[2:4]
        ret_mm = ret_date[5:7]
        ret_dd = ret_date[8:10]
        ret_yymmdd = f"{ret_yy}{ret_mm}{ret_dd}"
        ret_nodash = ret_date.replace("-", "")
    else:
        ret_yymmdd = ""
        ret_nodash = ""

    skyscanner = (
        f"https://www.skyscanner.co.kr/transport/flights/"
        f"{origin.lower()}/{destination.lower()}/"
        f"{dep_yymmdd}/"
        f"{ret_yymmdd if ret_date else ''}/"
        f"?adults=1&currency=KRW"
    )

    google = (
        f"https://www.google.com/travel/flights/search"
        f"?q=flights+from+{origin}+to+{destination}+on+{dep_date}"
    )

    naver = (
        f"https://flight.naver.com/flights/"
        f"{origin}-{destination}-{dep_nodash}/?adult=1"
        + (f"&returnDate={ret_nodash}" if ret_date else "")
    )

    tripdotcom = (
        f"https://kr.trip.com/flights/show"
        f"?dcity={origin}&acity={destination}"
        f"&ddate={dep_date}"
        + (f"&rdate={ret_date}" if ret_date else "")
        + f"&flightway={'RT' if ret_date else 'OW'}"
    )

    return {
        "skyscanner": skyscanner,
        "google": google,
        "naver": naver,
        "tripdotcom": tripdotcom,
    }
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd backend && uv run pytest tests/test_deep_links.py -v
```

Expected: PASS (8 tests)

- [ ] **Step 5: NotificationMessage에 deep_links 추가**

`backend/app/notify/base.py` 수정 — `NotificationMessage` 클래스에 필드 추가:
```python
class NotificationMessage(BaseModel):
    severity: Literal["info", "good", "great"]
    confidence: Confidence
    title: str
    summary: str
    fields: list[Field]
    link: str | None = None
    link_label: str | None = None
    deep_links: dict[str, str] | None = None   # ← 추가
    dashboard_url: str | None = None
    dedup_key: str
```

- [ ] **Step 6: slack.py에 딥링크 버튼 렌더링 추가**

`backend/app/notify/slack.py`의 `_build_blocks` 함수에서 기존 actions 블록 뒤에 추가:
```python
    if msg.deep_links:
        _LABEL_MAP = {
            "skyscanner": "스카이스캐너",
            "google": "구글항공",
            "naver": "네이버항공",
            "tripdotcom": "트립닷컴",
        }
        elements = [
            {
                "type": "button",
                "text": {"type": "plain_text", "text": _LABEL_MAP.get(k, k)},
                "url": v,
            }
            for k, v in msg.deep_links.items()
            if v
        ]
        if elements:
            blocks.append({"type": "actions", "elements": elements})
```

- [ ] **Step 7: OfferOut에 deep_links 필드 추가**

`backend/app/schemas/watch.py`의 `OfferOut` 클래스에 필드 추가:
```python
class OfferOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    watch_id: uuid.UUID
    source: str
    price_krw: int
    price_original: Decimal | None = None
    currency: str | None = Field(None, validation_alias="currency_original")
    depart_date: date | None = None
    depart_time: time | None = None
    return_date: date | None = None
    return_time: time | None = None
    airline: str | None = Field(None, validation_alias="carrier")
    deep_link: str | None = None
    deep_links: dict[str, str] | None = None   # ← 추가 (ORM 속성 아님, 라우터에서 주입)
    freshness: str
    collected_at: datetime
```

- [ ] **Step 8: get_offers 엔드포인트에서 deep_links 주입**

`backend/app/api/routes/watches.py`의 `get_offers` 함수 수정:

1. 파일 상단 import에 `build_deep_links` 추가:
```python
from app.utils.deep_links import build_deep_links
```

2. `get_offers` 함수 끝부분 변경 — Watch를 먼저 조회한 뒤 deep_links 주입:
```python
@router.get("/{watch_id}/offers", response_model=list[OfferOut], dependencies=[_auth])
async def get_offers(
    watch_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
):
    watch = await db.get(Watch, watch_id)
    if watch is None:
        raise HTTPException(status_code=404, detail="watch not found")

    latest_subq = (
        select(
            Offer.source,
            Offer.external_id,
            func.max(Offer.collected_at).label("max_at"),
        )
        .where(Offer.watch_id == watch_id)
        .group_by(Offer.source, Offer.external_id)
        .subquery()
    )
    result = await db.execute(
        select(Offer)
        .join(
            latest_subq,
            and_(
                Offer.source == latest_subq.c.source,
                Offer.external_id == latest_subq.c.external_id,
                Offer.collected_at == latest_subq.c.max_at,
                Offer.watch_id == watch_id,
            ),
        )
        .order_by(Offer.price_krw)
    )
    offers = result.scalars().all()

    # Watch params에서 딥링크 재료 추출 (flight 종류만)
    params = watch.params
    origin = params.get("origin") if isinstance(params, dict) else None
    destination = params.get("destination") if isinstance(params, dict) else None
    depart_from = params.get("depart_from") if isinstance(params, dict) else None

    out_list: list[OfferOut] = []
    for offer in offers:
        out = OfferOut.model_validate(offer)
        if origin and destination:
            dep = str(offer.depart_date) if offer.depart_date else depart_from
            ret = str(offer.return_date) if offer.return_date else None
            if dep:
                out.deep_links = build_deep_links(origin, destination, dep, ret)
        out_list.append(out)
    return out_list
```

- [ ] **Step 9: 기존 테스트 통과 확인**

```bash
cd backend && uv run pytest -q
```

Expected: 148+ tests PASS (기존 테스트 회귀 없음)

- [ ] **Step 10: 커밋**

```bash
git add backend/app/utils/ backend/app/notify/base.py backend/app/notify/slack.py \
        backend/app/schemas/watch.py backend/app/api/routes/watches.py \
        backend/tests/test_deep_links.py
git commit -m "feat: B — 딥링크 유틸 + OfferOut deep_links + 슬랙 버튼 4개"
```

---

### Task 2: B-프론트엔드 — OfferList 딥링크 버튼

**Files:**
- Modify: `web/lib/types.ts:92-105` (OfferOut에 deep_links 추가)
- Modify: `web/components/watch-detail/OfferList.tsx` (딥링크 버튼 4개)

**Interfaces:**
- Consumes: Task 1의 `OfferOut.deep_links: dict<string, string> | null`

- [ ] **Step 1: types.ts 업데이트**

`web/lib/types.ts`의 `OfferOut` 인터페이스에 추가:
```typescript
export interface OfferOut {
  id: string;
  watch_id: string;
  price_krw: number;
  price_original: number | null;
  currency: string | null;
  depart_date: string | null;
  return_date: string | null;
  depart_time: string | null;   // ← 있으면 표시용
  return_time: string | null;   // ← 있으면 표시용
  airline: string | null;
  deep_link: string | null;
  deep_links: Record<string, string> | null;   // ← 추가
  freshness: Freshness;
  source: string;
  collected_at: string;
}
```

- [ ] **Step 2: OfferList 컴포넌트에 딥링크 버튼 추가**

`web/components/watch-detail/OfferList.tsx`의 각 오퍼 카드 하단(freshness/source 뱃지 아래)에 추가:

```tsx
{offer.deep_links && (
  <div className="flex flex-wrap gap-2 mt-2">
    {[
      { key: 'skyscanner', label: '스카이스캐너' },
      { key: 'google',     label: '구글항공' },
      { key: 'naver',      label: '네이버항공' },
      { key: 'tripdotcom', label: '트립닷컴' },
    ].map(({ key, label }) =>
      offer.deep_links?.[key] ? (
        <a
          key={key}
          href={offer.deep_links[key]}
          target="_blank"
          rel="noopener noreferrer"
          className="text-xs border rounded px-2 py-1 text-gray-600 hover:bg-gray-50"
        >
          {label}에서 검색
        </a>
      ) : null
    )}
  </div>
)}
```

- [ ] **Step 3: 타입체크**

```bash
cd web && npm run typecheck
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add web/lib/types.ts web/components/watch-detail/OfferList.tsx
git commit -m "feat: B — OfferList 딥링크 버튼 4개 추가"
```

---

### Task 3: DB 마이그레이션 — airline_events 테이블 + AirlineEvent 모델

**Files:**
- Create: `backend/app/models/event.py`
- Modify: `backend/app/models/__init__.py` (AirlineEvent import)
- Create: `backend/alembic/versions/<hash>_add_airline_events.py`

**Interfaces:**
- Produces: `AirlineEvent` SQLAlchemy 모델 (Task 4, 5가 의존)

- [ ] **Step 1: AirlineEvent 모델 작성**

`backend/app/models/event.py`:
```python
"""항공사 공식 이벤트 페이지에서 수집된 특가 이벤트."""

from __future__ import annotations

import uuid
from datetime import date, datetime

from sqlalchemy import Boolean, Date, Index, Text, UniqueConstraint
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column

from app.models.base import Base, created_at_col


class AirlineEvent(Base):
    __tablename__ = "airline_events"

    id: Mapped[uuid.UUID] = mapped_column(
        UUID(as_uuid=True), primary_key=True, default=uuid.uuid4
    )
    source: Mapped[str] = mapped_column(Text, nullable=False)
    external_id: Mapped[str] = mapped_column(Text, nullable=False)
    title: Mapped[str] = mapped_column(Text, nullable=False)
    url: Mapped[str] = mapped_column(Text, nullable=False)
    origin: Mapped[str | None] = mapped_column(Text)
    destination: Mapped[str | None] = mapped_column(Text)
    valid_from: Mapped[date | None] = mapped_column(Date)
    valid_to: Mapped[date | None] = mapped_column(Date)
    discount_info: Mapped[str | None] = mapped_column(Text)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)
    fetched_at: Mapped[datetime] = created_at_col()

    __table_args__ = (
        UniqueConstraint("source", "external_id", name="uq_airline_events_source_ext"),
        Index("ix_airline_events_source", "source"),
        Index("ix_airline_events_fetched", "fetched_at"),
    )
```

- [ ] **Step 2: models/__init__.py에 import 추가**

`backend/app/models/__init__.py`를 열어 AirlineEvent를 추가한다.
파일이 없으면 생성:
```python
from app.models.event import AirlineEvent
from app.models.price import Offer, PriceSnapshot
from app.models.watch import Watch, WatchRun

__all__ = ["AirlineEvent", "Offer", "PriceSnapshot", "Watch", "WatchRun"]
```

파일이 있으면 `AirlineEvent` 관련 줄만 추가.

- [ ] **Step 3: alembic 마이그레이션 생성**

도커 백엔드가 실행 중인 상태에서:
```bash
cd backend && uv run alembic revision --autogenerate -m "add_airline_events"
```

생성된 파일을 열어 `upgrade()`/`downgrade()`가 올바른지 확인. 기대 내용:

```python
def upgrade() -> None:
    op.create_table(
        "airline_events",
        sa.Column("id", sa.dialects.postgresql.UUID(as_uuid=True), primary_key=True),
        sa.Column("source", sa.Text(), nullable=False),
        sa.Column("external_id", sa.Text(), nullable=False),
        sa.Column("title", sa.Text(), nullable=False),
        sa.Column("url", sa.Text(), nullable=False),
        sa.Column("origin", sa.Text(), nullable=True),
        sa.Column("destination", sa.Text(), nullable=True),
        sa.Column("valid_from", sa.Date(), nullable=True),
        sa.Column("valid_to", sa.Date(), nullable=True),
        sa.Column("discount_info", sa.Text(), nullable=True),
        sa.Column("is_active", sa.Boolean(), nullable=False),
        sa.Column("fetched_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
        sa.UniqueConstraint("source", "external_id", name="uq_airline_events_source_ext"),
    )
    op.create_index("ix_airline_events_source", "airline_events", ["source"])
    op.create_index("ix_airline_events_fetched", "airline_events", ["fetched_at"])


def downgrade() -> None:
    op.drop_index("ix_airline_events_fetched", "airline_events")
    op.drop_index("ix_airline_events_source", "airline_events")
    op.drop_table("airline_events")
```

- [ ] **Step 4: 마이그레이션 적용**

```bash
cd backend && uv run alembic upgrade head
```

Expected: 에러 없이 완료

- [ ] **Step 5: 테이블 확인**

```bash
docker compose exec db psql -U trippick -d trippick -c "\d airline_events"
```

Expected: 컬럼 목록 출력

- [ ] **Step 6: 커밋**

```bash
git add backend/app/models/event.py backend/app/models/__init__.py \
        backend/alembic/versions/
git commit -m "feat: DB — airline_events 테이블 추가 (Alembic 마이그레이션)"
```

---

### Task 4: D-백엔드 — EventAdapter 기반 + 파서 5개 (TDD)

**Files:**
- Modify: `backend/app/sources/policy.py` (이벤트 도메인 허용 추가)
- Create: `backend/app/sources/events/__init__.py`
- Create: `backend/app/sources/events/base.py`
- Create: `backend/app/sources/events/airbusan.py`
- Create: `backend/app/sources/events/tway.py`
- Create: `backend/app/sources/events/jejuair.py`
- Create: `backend/app/sources/events/koreanair.py`
- Create: `backend/app/sources/events/flightdeal.py`
- Create: `backend/tests/fixtures/events/airbusan.html`
- Create: `backend/tests/fixtures/events/tway.html`
- Create: `backend/tests/fixtures/events/jejuair.html`
- Create: `backend/tests/fixtures/events/koreanair.html`
- Create: `backend/tests/fixtures/events/flightdeal.html`
- Create: `backend/tests/sources/events/test_parsers.py`

**Interfaces:**
- Consumes: `RateLimitedClient` (sources/http.py), `CrawlPolicy` (sources/policy.py)
- Produces: `EventAdapter`, `AirlineEventData` (Task 5가 의존)

- [ ] **Step 1: policy.py에 이벤트 도메인 허용 추가**

`backend/app/sources/policy.py` 수정:
```python
from __future__ import annotations

from app.config import get_settings

_ALLOWED_EVENT_DOMAINS = frozenset({
    "www.airbusan.com",
    "www.twayair.com",
    "www.jejuair.net",
    "www.koreanair.com",
    "flightdeal.kr",
})


class CrawlPolicy:
    """크롤러 활성화 게이트."""

    @staticmethod
    def require_enabled() -> None:
        if not get_settings().crawl_enabled:
            raise RuntimeError(
                "CRAWL_ENABLED is false — crawl adapters are disabled. "
                "Set CRAWL_ENABLED=true in .env to enable."
            )

    @staticmethod
    def check_allowed(domain: str, path: str = "/") -> bool:
        """robots.txt: 수동 검증된 이벤트 도메인만 허용."""
        return domain in _ALLOWED_EVENT_DOMAINS
```

- [ ] **Step 2: 실패하는 파서 테스트 작성**

먼저 fixtures 디렉토리 생성 및 최소한의 HTML fixture 파일 만들기.

`backend/tests/fixtures/events/airbusan.html` — 에어부산 이벤트 목록 페이지를 닮은 최소 HTML:
```html
<!DOCTYPE html>
<html lang="ko">
<body>
<ul class="event-list">
  <li class="event-item">
    <a href="/ko/flyNEvent/detail?eventId=EVT2026001">
      <span class="event-title">에어부산 추석 특가 최대 40% 할인</span>
    </a>
    <span class="event-period">2026.09.15 ~ 2026.09.30</span>
  </li>
  <li class="event-item">
    <a href="/ko/flyNEvent/detail?eventId=EVT2026002">
      <span class="event-title">일본 노선 특가 이벤트</span>
    </a>
    <span class="event-period">2026.10.01 ~ 2026.10.31</span>
  </li>
</ul>
</body>
</html>
```

`backend/tests/fixtures/events/tway.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<body>
<div class="event-list">
  <div class="event-card">
    <a href="/app/promotion/event/detail?eventId=TW2026001">
      <p class="event-name">티웨이 제주 특가 39,900원부터</p>
    </a>
    <span class="period">2026.09.10 ~ 2026.09.20</span>
  </div>
</div>
</body>
</html>
```

`backend/tests/fixtures/events/jejuair.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<body>
<div id="eventList">
  <div class="event-item">
    <a href="/ko/event/event.do?eventNo=JJ20260001">
      <strong class="tit">제주항공 가을 얼리버드</strong>
    </a>
    <span class="date">2026.09.01 ~ 2026.09.30</span>
  </div>
</div>
</body>
</html>
```

`backend/tests/fixtures/events/koreanair.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<body>
<div class="promotion-list">
  <article class="promotion-item">
    <a href="/kr/ko/promotion/detail?promotionId=KAL2026001">
      <h3 class="promotion-title">대한항공 마일리지 보너스 항공권</h3>
    </a>
    <p class="promotion-period">2026.09.01 ~ 2026.09.30</p>
  </article>
</div>
</body>
</html>
```

`backend/tests/fixtures/events/flightdeal.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<body>
<div class="deal-list">
  <div class="deal-item" id="deal-FD2026001">
    <a href="/deals/FD2026001">
      <h2 class="deal-title">김포-제주 왕복 59,000원 (이스타항공)</h2>
    </a>
    <span class="valid-date">2026.09.20까지</span>
  </div>
</div>
</body>
</html>
```

`backend/tests/sources/events/__init__.py` (빈 파일)

`backend/tests/sources/__init__.py` (없으면 생성, 빈 파일)

`backend/tests/sources/events/test_parsers.py`:
```python
"""EventAdapter 파서 단위 테스트 — 실제 HTTP 없음, fixture HTML만 사용."""

from pathlib import Path
import pytest
from app.sources.events.airbusan import AirBusanAdapter
from app.sources.events.tway import TwayAdapter
from app.sources.events.jejuair import JejuAirAdapter
from app.sources.events.koreanair import KoreanAirAdapter
from app.sources.events.flightdeal import FlightDealAdapter

FIXTURES = Path(__file__).parent.parent.parent / "fixtures" / "events"


def _html(name: str) -> str:
    return (FIXTURES / name).read_text(encoding="utf-8")


class TestAirBusanParser:
    def test_returns_list(self):
        events = AirBusanAdapter._parse_html(_html("airbusan.html"))
        assert isinstance(events, list)

    def test_finds_events(self):
        events = AirBusanAdapter._parse_html(_html("airbusan.html"))
        assert len(events) >= 1

    def test_event_has_required_fields(self):
        events = AirBusanAdapter._parse_html(_html("airbusan.html"))
        e = events[0]
        assert e.source == "airbusan"
        assert e.external_id
        assert e.title
        assert e.url
        assert "airbusan" in e.url or e.url.startswith("http")

    def test_event_has_valid_to(self):
        events = AirBusanAdapter._parse_html(_html("airbusan.html"))
        # valid_to가 있으면 date 타입
        if events[0].valid_to is not None:
            from datetime import date
            assert isinstance(events[0].valid_to, date)


class TestTwayParser:
    def test_returns_list(self):
        events = TwayAdapter._parse_html(_html("tway.html"))
        assert len(events) >= 1

    def test_source_is_tway(self):
        events = TwayAdapter._parse_html(_html("tway.html"))
        assert all(e.source == "tway" for e in events)


class TestJejuAirParser:
    def test_returns_list(self):
        events = JejuAirAdapter._parse_html(_html("jejuair.html"))
        assert len(events) >= 1

    def test_source_is_jejuair(self):
        events = JejuAirAdapter._parse_html(_html("jejuair.html"))
        assert all(e.source == "jejuair" for e in events)


class TestKoreanAirParser:
    def test_returns_list(self):
        events = KoreanAirAdapter._parse_html(_html("koreanair.html"))
        assert len(events) >= 1

    def test_source_is_koreanair(self):
        events = KoreanAirAdapter._parse_html(_html("koreanair.html"))
        assert all(e.source == "koreanair" for e in events)


class TestFlightDealParser:
    def test_returns_list(self):
        events = FlightDealAdapter._parse_html(_html("flightdeal.html"))
        assert len(events) >= 1

    def test_source_is_flightdeal(self):
        events = FlightDealAdapter._parse_html(_html("flightdeal.html"))
        assert all(e.source == "flightdeal" for e in events)
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
cd backend && uv run pytest tests/sources/events/test_parsers.py -v
```

Expected: FAIL (모듈 없음)

- [ ] **Step 4: EventAdapter 기반 클래스 구현**

`backend/app/sources/events/__init__.py` (빈 파일)

`backend/app/sources/events/base.py`:
```python
"""EventAdapter 추상 기반 클래스."""

from __future__ import annotations

import hashlib
from abc import ABC, abstractmethod
from dataclasses import dataclass
from datetime import date

import structlog

from app.sources.http import RateLimitedClient
from app.sources.policy import CrawlPolicy

log = structlog.get_logger()


@dataclass
class AirlineEventData:
    source: str
    external_id: str
    title: str
    url: str
    origin: str | None = None
    destination: str | None = None
    valid_from: date | None = None
    valid_to: date | None = None
    discount_info: str | None = None


def url_to_external_id(url: str) -> str:
    """URL에서 이벤트 번호를 추출 못 할 때 SHA256 앞 16자 사용."""
    return hashlib.sha256(url.encode()).hexdigest()[:16]


class EventAdapter(ABC):
    source: str  # 서브클래스에서 선언
    base_url: str  # 이벤트 목록 URL

    def __init__(self, client: RateLimitedClient | None = None) -> None:
        self._client = client or RateLimitedClient(min_interval_sec=2.0)

    async def fetch_events(self) -> list[AirlineEventData]:
        """policy.py 게이트 → HTTP → 파싱. 예외는 빈 리스트로 흡수."""
        from urllib.parse import urlparse

        CrawlPolicy.require_enabled()
        domain = urlparse(self.base_url).netloc
        if not CrawlPolicy.check_allowed(domain):
            log.warning("event_adapter.domain_not_allowed", source=self.source, domain=domain)
            return []
        try:
            resp = await self._client.get(
                self.base_url,
                headers={"User-Agent": "TripPickBot/1.0 (+https://github.com/soominok/trip_pick)"},
            )
            resp.raise_for_status()
            return self._parse_html(resp.text)
        except Exception:
            log.exception("event_adapter.fetch_failed", source=self.source)
            return []

    @classmethod
    @abstractmethod
    def _parse_html(cls, html: str) -> list[AirlineEventData]:
        """HTML 문자열 → AirlineEventData 리스트. 예외 없이 빈 리스트 반환 가능."""
        ...
```

- [ ] **Step 5: BeautifulSoup4 의존성 확인**

```bash
cd backend && uv add beautifulsoup4 lxml
```

pyproject.toml에 추가됐는지 확인.

- [ ] **Step 6: 파서 5개 구현**

`backend/app/sources/events/airbusan.py`:
```python
from __future__ import annotations

import re
from datetime import date

from bs4 import BeautifulSoup

from app.sources.events.base import AirlineEventData, EventAdapter, url_to_external_id

_BASE = "https://www.airbusan.com"


class AirBusanAdapter(EventAdapter):
    source = "airbusan"
    base_url = f"{_BASE}/ko/flyNEvent/"

    @classmethod
    def _parse_html(cls, html: str) -> list[AirlineEventData]:
        soup = BeautifulSoup(html, "lxml")
        events: list[AirlineEventData] = []
        for item in soup.select(".event-item"):
            a = item.select_one("a[href]")
            if not a:
                continue
            title_el = a.select_one(".event-title") or a
            title = title_el.get_text(strip=True)
            href = a["href"]
            url = href if href.startswith("http") else f"{_BASE}{href}"
            # external_id: eventId 파라미터 추출 시도
            m = re.search(r"eventId=([A-Za-z0-9_-]+)", href)
            ext_id = m.group(1) if m else url_to_external_id(url)
            # 기간 파싱
            period_el = item.select_one(".event-period")
            valid_from, valid_to = _parse_period(period_el.get_text(strip=True) if period_el else "")
            events.append(AirlineEventData(
                source=cls.source,
                external_id=ext_id,
                title=title,
                url=url,
                valid_from=valid_from,
                valid_to=valid_to,
            ))
        return events


def _parse_period(text: str) -> tuple[date | None, date | None]:
    """'2026.09.15 ~ 2026.09.30' 형태 파싱."""
    dates = re.findall(r"(\d{4})[./](\d{2})[./](\d{2})", text)
    if len(dates) >= 2:
        return (_to_date(*dates[0]), _to_date(*dates[1]))
    if len(dates) == 1:
        return (None, _to_date(*dates[0]))
    return (None, None)


def _to_date(y: str, m: str, d: str) -> date:
    return date(int(y), int(m), int(d))
```

`backend/app/sources/events/tway.py`:
```python
from __future__ import annotations

import re
from datetime import date

from bs4 import BeautifulSoup

from app.sources.events.base import AirlineEventData, EventAdapter, url_to_external_id

_BASE = "https://www.twayair.com"


class TwayAdapter(EventAdapter):
    source = "tway"
    base_url = f"{_BASE}/app/promotion/event/being"

    @classmethod
    def _parse_html(cls, html: str) -> list[AirlineEventData]:
        soup = BeautifulSoup(html, "lxml")
        events: list[AirlineEventData] = []
        for card in soup.select(".event-card"):
            a = card.select_one("a[href]")
            if not a:
                continue
            name_el = a.select_one(".event-name") or a
            title = name_el.get_text(strip=True)
            href = a["href"]
            url = href if href.startswith("http") else f"{_BASE}{href}"
            m = re.search(r"eventId=([A-Za-z0-9_-]+)", href)
            ext_id = m.group(1) if m else url_to_external_id(url)
            period_el = card.select_one(".period")
            valid_from, valid_to = _parse_period(period_el.get_text(strip=True) if period_el else "")
            events.append(AirlineEventData(
                source=cls.source,
                external_id=ext_id,
                title=title,
                url=url,
                valid_from=valid_from,
                valid_to=valid_to,
            ))
        return events


def _parse_period(text: str) -> tuple[date | None, date | None]:
    dates = re.findall(r"(\d{4})[./](\d{2})[./](\d{2})", text)
    if len(dates) >= 2:
        return (date(int(dates[0][0]), int(dates[0][1]), int(dates[0][2])),
                date(int(dates[1][0]), int(dates[1][1]), int(dates[1][2])))
    if len(dates) == 1:
        return (None, date(int(dates[0][0]), int(dates[0][1]), int(dates[0][2])))
    return (None, None)
```

`backend/app/sources/events/jejuair.py`:
```python
from __future__ import annotations

import re
from datetime import date

from bs4 import BeautifulSoup

from app.sources.events.base import AirlineEventData, EventAdapter, url_to_external_id

_BASE = "https://www.jejuair.net"


class JejuAirAdapter(EventAdapter):
    source = "jejuair"
    base_url = f"{_BASE}/ko/event/event.do"

    @classmethod
    def _parse_html(cls, html: str) -> list[AirlineEventData]:
        soup = BeautifulSoup(html, "lxml")
        events: list[AirlineEventData] = []
        for item in soup.select("#eventList .event-item, .event-list .event-item"):
            a = item.select_one("a[href]")
            if not a:
                continue
            title_el = a.select_one(".tit, strong") or a
            title = title_el.get_text(strip=True)
            href = a["href"]
            url = href if href.startswith("http") else f"{_BASE}{href}"
            m = re.search(r"eventNo=([A-Za-z0-9_-]+)", href)
            ext_id = m.group(1) if m else url_to_external_id(url)
            date_el = item.select_one(".date, .period")
            valid_from, valid_to = _parse_period(date_el.get_text(strip=True) if date_el else "")
            events.append(AirlineEventData(
                source=cls.source,
                external_id=ext_id,
                title=title,
                url=url,
                valid_from=valid_from,
                valid_to=valid_to,
            ))
        return events


def _parse_period(text: str) -> tuple[date | None, date | None]:
    dates = re.findall(r"(\d{4})[./](\d{2})[./](\d{2})", text)
    if len(dates) >= 2:
        return (date(int(dates[0][0]), int(dates[0][1]), int(dates[0][2])),
                date(int(dates[1][0]), int(dates[1][1]), int(dates[1][2])))
    if len(dates) == 1:
        return (None, date(int(dates[0][0]), int(dates[0][1]), int(dates[0][2])))
    return (None, None)
```

`backend/app/sources/events/koreanair.py`:
```python
from __future__ import annotations

import re
from datetime import date

from bs4 import BeautifulSoup

from app.sources.events.base import AirlineEventData, EventAdapter, url_to_external_id

_BASE = "https://www.koreanair.com"


class KoreanAirAdapter(EventAdapter):
    source = "koreanair"
    base_url = f"{_BASE}/kr/ko/promotion/list"

    @classmethod
    def _parse_html(cls, html: str) -> list[AirlineEventData]:
        soup = BeautifulSoup(html, "lxml")
        events: list[AirlineEventData] = []
        for article in soup.select(".promotion-item, article.promotion-item"):
            a = article.select_one("a[href]")
            if not a:
                continue
            title_el = article.select_one(".promotion-title, h3") or a
            title = title_el.get_text(strip=True)
            href = a["href"]
            url = href if href.startswith("http") else f"{_BASE}{href}"
            m = re.search(r"promotionId=([A-Za-z0-9_-]+)", href)
            ext_id = m.group(1) if m else url_to_external_id(url)
            period_el = article.select_one(".promotion-period, p.period")
            valid_from, valid_to = _parse_period(period_el.get_text(strip=True) if period_el else "")
            events.append(AirlineEventData(
                source=cls.source,
                external_id=ext_id,
                title=title,
                url=url,
                valid_from=valid_from,
                valid_to=valid_to,
            ))
        return events


def _parse_period(text: str) -> tuple[date | None, date | None]:
    dates = re.findall(r"(\d{4})[./](\d{2})[./](\d{2})", text)
    if len(dates) >= 2:
        return (date(int(dates[0][0]), int(dates[0][1]), int(dates[0][2])),
                date(int(dates[1][0]), int(dates[1][1]), int(dates[1][2])))
    if len(dates) == 1:
        return (None, date(int(dates[0][0]), int(dates[0][1]), int(dates[0][2])))
    return (None, None)
```

`backend/app/sources/events/flightdeal.py`:
```python
from __future__ import annotations

import re
from datetime import date

from bs4 import BeautifulSoup

from app.sources.events.base import AirlineEventData, EventAdapter, url_to_external_id

_BASE = "https://flightdeal.kr"


class FlightDealAdapter(EventAdapter):
    source = "flightdeal"
    base_url = _BASE

    @classmethod
    def _parse_html(cls, html: str) -> list[AirlineEventData]:
        soup = BeautifulSoup(html, "lxml")
        events: list[AirlineEventData] = []
        for item in soup.select(".deal-item"):
            a = item.select_one("a[href]")
            if not a:
                continue
            title_el = item.select_one(".deal-title, h2") or a
            title = title_el.get_text(strip=True)
            href = a["href"]
            url = href if href.startswith("http") else f"{_BASE}{href}"
            # external_id: deal item의 id 속성 또는 URL 해시
            item_id = item.get("id", "")
            ext_id = item_id.replace("deal-", "") if item_id.startswith("deal-") else url_to_external_id(url)
            date_el = item.select_one(".valid-date, .period")
            _, valid_to = _parse_period(date_el.get_text(strip=True) if date_el else "")
            events.append(AirlineEventData(
                source=cls.source,
                external_id=ext_id,
                title=title,
                url=url,
                valid_to=valid_to,
            ))
        return events


def _parse_period(text: str) -> tuple[date | None, date | None]:
    dates = re.findall(r"(\d{4})[./](\d{2})[./](\d{2})", text)
    if len(dates) >= 2:
        return (date(int(dates[0][0]), int(dates[0][1]), int(dates[0][2])),
                date(int(dates[1][0]), int(dates[1][1]), int(dates[1][2])))
    if len(dates) == 1:
        return (None, date(int(dates[0][0]), int(dates[0][1]), int(dates[0][2])))
    return (None, None)
```

- [ ] **Step 7: 파서 테스트 통과 확인**

```bash
cd backend && uv run pytest tests/sources/events/test_parsers.py -v
```

Expected: PASS (20 tests)

- [ ] **Step 8: 전체 테스트 회귀 확인**

```bash
cd backend && uv run pytest -q
```

Expected: 148+ tests PASS

- [ ] **Step 9: 커밋**

```bash
git add backend/app/sources/policy.py backend/app/sources/events/ \
        backend/tests/fixtures/events/ backend/tests/sources/
git commit -m "feat: D — EventAdapter 기반 + 파서 5개 (에어부산/티웨이/제주/대한/flightdeal)"
```

---

### Task 5: D-백엔드 — event_tick Worker + /api/events 엔드포인트

**Files:**
- Create: `backend/app/schemas/event.py`
- Create: `backend/app/api/routes/events.py`
- Modify: `backend/app/main.py` (events_router 등록)
- Modify: `backend/app/worker.py` (event_tick 추가)
- Create: `backend/tests/test_event_worker.py`

**Interfaces:**
- Consumes: `AirlineEvent` (Task 3), `EventAdapter` + `AirlineEventData` (Task 4)
- Produces: `GET /api/events` 엔드포인트

- [ ] **Step 1: EventOut 스키마 작성**

`backend/app/schemas/event.py`:
```python
"""이벤트 응답 DTO."""

from __future__ import annotations

import uuid
from datetime import date, datetime

from pydantic import BaseModel, ConfigDict


class EventOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    source: str
    external_id: str
    title: str
    url: str
    origin: str | None = None
    destination: str | None = None
    valid_from: date | None = None
    valid_to: date | None = None
    discount_info: str | None = None
    is_active: bool
    fetched_at: datetime
```

- [ ] **Step 2: 실패하는 worker 테스트 작성**

`backend/tests/test_event_worker.py`:
```python
"""event_tick 단위 테스트 — 실제 HTTP 없음."""

import pytest
from unittest.mock import AsyncMock, MagicMock, patch
from sqlalchemy.ext.asyncio import AsyncSession

from app.sources.events.base import AirlineEventData
from datetime import date


@pytest.mark.asyncio
async def test_event_tick_inserts_new_event(async_session: AsyncSession):
    """신규 이벤트가 DB에 삽입되는지 확인."""
    from app.worker import event_tick

    fake_event = AirlineEventData(
        source="airbusan",
        external_id="EVT_TEST_001",
        title="테스트 특가",
        url="https://www.airbusan.com/ko/flyNEvent/detail?eventId=EVT_TEST_001",
        valid_to=date(2026, 12, 31),
    )

    with patch("app.worker.build_event_adapters") as mock_adapters:
        adapter_mock = AsyncMock()
        adapter_mock.fetch_events = AsyncMock(return_value=[fake_event])
        mock_adapters.return_value = [adapter_mock]

        with patch("app.worker.SessionLocal") as mock_session_cls:
            mock_session_cls.return_value.__aenter__ = AsyncMock(return_value=async_session)
            mock_session_cls.return_value.__aexit__ = AsyncMock(return_value=False)
            await event_tick()

    # DB에 이벤트가 생성됐는지 확인
    from sqlalchemy import select
    from app.models.event import AirlineEvent
    result = await async_session.execute(
        select(AirlineEvent).where(AirlineEvent.external_id == "EVT_TEST_001")
    )
    row = result.scalar_one_or_none()
    assert row is not None
    assert row.title == "테스트 특가"
    assert row.source == "airbusan"


@pytest.mark.asyncio
async def test_event_tick_dedup_same_event(async_session: AsyncSession):
    """동일 (source, external_id)는 중복 저장 안 됨."""
    from app.worker import event_tick

    fake_event = AirlineEventData(
        source="tway",
        external_id="TW_DEDUP_001",
        title="중복 테스트",
        url="https://www.twayair.com/app/promotion/event/detail?eventId=TW_DEDUP_001",
    )

    for _ in range(2):
        with patch("app.worker.build_event_adapters") as mock_adapters:
            adapter_mock = AsyncMock()
            adapter_mock.fetch_events = AsyncMock(return_value=[fake_event])
            mock_adapters.return_value = [adapter_mock]

            with patch("app.worker.SessionLocal") as mock_session_cls:
                mock_session_cls.return_value.__aenter__ = AsyncMock(return_value=async_session)
                mock_session_cls.return_value.__aexit__ = AsyncMock(return_value=False)
                await event_tick()

    from sqlalchemy import select, func
    from app.models.event import AirlineEvent
    result = await async_session.execute(
        select(func.count()).where(
            AirlineEvent.source == "tway",
            AirlineEvent.external_id == "TW_DEDUP_001"
        )
    )
    assert result.scalar() == 1
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
cd backend && uv run pytest tests/test_event_worker.py -v
```

Expected: FAIL (`event_tick`, `build_event_adapters` 없음)

- [ ] **Step 4: worker.py에 event_tick 추가**

`backend/app/worker.py` 수정 — `source_health_check` 함수 뒤에 추가:

```python
from app.models.event import AirlineEvent
from app.sources.events.airbusan import AirBusanAdapter
from app.sources.events.tway import TwayAdapter
from app.sources.events.jejuair import JejuAirAdapter
from app.sources.events.koreanair import KoreanAirAdapter
from app.sources.events.flightdeal import FlightDealAdapter


def build_event_adapters():
    return [
        AirBusanAdapter(),
        TwayAdapter(),
        JejuAirAdapter(),
        KoreanAirAdapter(),
        FlightDealAdapter(),
    ]


async def event_tick() -> None:
    """항공사 이벤트 페이지 수집. 6시간마다 실행."""
    settings = get_settings()
    if not settings.crawl_enabled:
        log.info("worker.event_tick.skipped", reason="CRAWL_ENABLED=false")
        return

    adapters = build_event_adapters()
    new_count = 0

    for adapter in adapters:
        events = await adapter.fetch_events()
        if not events:
            continue

        async with SessionLocal() as session:
            for ev in events:
                from sqlalchemy.dialects.postgresql import insert as pg_insert
                stmt = (
                    pg_insert(AirlineEvent)
                    .values(
                        source=ev.source,
                        external_id=ev.external_id,
                        title=ev.title,
                        url=ev.url,
                        origin=ev.origin,
                        destination=ev.destination,
                        valid_from=ev.valid_from,
                        valid_to=ev.valid_to,
                        discount_info=ev.discount_info,
                        is_active=True,
                    )
                    .on_conflict_do_nothing(constraint="uq_airline_events_source_ext")
                )
                result = await session.execute(stmt)
                if result.rowcount:
                    new_count += 1
            await session.commit()

    log.info("worker.event_tick.done", new_events=new_count)
```

그리고 `main()` 함수의 scheduler 설정에 추가:
```python
scheduler.add_job(
    event_tick, "interval", hours=6, id="event_tick", max_instances=1, coalesce=True
)
```

- [ ] **Step 5: /api/events 라우터 작성**

`backend/app/api/routes/events.py`:
```python
"""항공사 이벤트 CRUD."""

from __future__ import annotations

from fastapi import APIRouter, Depends
from sqlalchemy import desc, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.deps import get_db, require_token
from app.models.event import AirlineEvent
from app.schemas.event import EventOut

router = APIRouter(prefix="/api/events", tags=["events"])
_auth = Depends(require_token)


@router.get("", response_model=list[EventOut], dependencies=[_auth])
async def list_events(
    source: str | None = None,
    active_only: bool = True,
    limit: int = 100,
    db: AsyncSession = Depends(get_db),
):
    q = select(AirlineEvent).order_by(desc(AirlineEvent.fetched_at)).limit(limit)
    if active_only:
        q = q.where(AirlineEvent.is_active.is_(True))
    if source:
        q = q.where(AirlineEvent.source == source)
    result = await db.execute(q)
    return result.scalars().all()
```

- [ ] **Step 6: main.py에 라우터 등록**

`backend/app/main.py` 수정:
```python
from app.api.routes.events import router as events_router
# ... 기존 import 유지 ...

app.include_router(events_router)  # watches_router, alerts_router 아래에 추가
```

- [ ] **Step 7: 테스트 통과 확인**

```bash
cd backend && uv run pytest tests/test_event_worker.py -v
cd backend && uv run pytest -q
```

Expected: 모든 테스트 PASS

- [ ] **Step 8: 동작 확인 (docker compose up 상태에서)**

```bash
curl -H "Authorization: Bearer $APP_API_TOKEN" http://localhost:8000/api/events
```

Expected: `[]` (아직 수집 전)

- [ ] **Step 9: 커밋**

```bash
git add backend/app/schemas/event.py backend/app/api/routes/events.py \
        backend/app/main.py backend/app/worker.py \
        backend/tests/test_event_worker.py
git commit -m "feat: D — event_tick Worker(6h) + GET /api/events 엔드포인트"
```

---

### Task 6: D-프론트엔드 — 이벤트 페이지 + Nav

**Files:**
- Modify: `web/lib/types.ts` (EventOut 타입 추가)
- Create: `web/hooks/useEvents.ts`
- Create: `web/app/events/page.tsx`
- Create: `web/app/events/components/EventCard.tsx`
- Modify: `web/components/layout/Nav.tsx` (이벤트 메뉴 추가)

**Interfaces:**
- Consumes: Task 5의 `GET /api/events?source=&active_only=true`

- [ ] **Step 1: types.ts에 EventOut 추가**

`web/lib/types.ts` 하단에 추가:
```typescript
export interface EventOut {
  id: string;
  source: string;
  external_id: string;
  title: string;
  url: string;
  origin: string | null;
  destination: string | null;
  valid_from: string | null;   // "YYYY-MM-DD"
  valid_to: string | null;
  discount_info: string | null;
  is_active: boolean;
  fetched_at: string;
}
```

- [ ] **Step 2: useEvents 훅 작성**

`web/hooks/useEvents.ts`:
```typescript
'use client';

import { useQuery } from '@tanstack/react-query';
import { apiFetch } from '@/lib/client';
import type { EventOut } from '@/lib/types';

export function useEvents(source?: string) {
  return useQuery<EventOut[]>({
    queryKey: ['events', source ?? 'all'],
    queryFn: () => {
      const params = new URLSearchParams({ active_only: 'true' });
      if (source) params.set('source', source);
      return apiFetch<EventOut[]>(`/api/events?${params}`);
    },
    staleTime: 30 * 60_000,  // 30분 캐시
  });
}
```

- [ ] **Step 3: EventCard 컴포넌트 작성**

`web/app/events/components/EventCard.tsx`:
```tsx
import type { EventOut } from '@/lib/types';

const SOURCE_LABELS: Record<string, string> = {
  airbusan: '에어부산',
  tway: '티웨이항공',
  jejuair: '제주항공',
  koreanair: '대한항공',
  flightdeal: 'flightdeal.kr',
};

function formatDate(d: string | null) {
  if (!d) return null;
  return d.replace(/-/g, '.').slice(2);  // "26.09.01"
}

export default function EventCard({ event }: { event: EventOut }) {
  const label = SOURCE_LABELS[event.source] ?? event.source;
  const period =
    event.valid_from && event.valid_to
      ? `${formatDate(event.valid_from)} ~ ${formatDate(event.valid_to)}`
      : event.valid_to
      ? `~ ${formatDate(event.valid_to)}`
      : null;

  return (
    <div className="rounded-xl border p-4 space-y-2 hover:shadow-sm transition-shadow">
      <div className="flex items-center gap-2">
        <span className="text-xs font-medium px-2 py-0.5 rounded-full bg-blue-100 text-blue-800">
          {label}
        </span>
        {period && (
          <span className="text-xs text-gray-500">{period}</span>
        )}
      </div>
      <p className="text-sm font-medium leading-snug">{event.title}</p>
      {event.discount_info && (
        <p className="text-xs text-green-700">{event.discount_info}</p>
      )}
      <a
        href={event.url}
        target="_blank"
        rel="noopener noreferrer"
        className="inline-block text-xs text-blue-600 hover:underline"
      >
        자세히 보기 →
      </a>
    </div>
  );
}
```

- [ ] **Step 4: 이벤트 목록 페이지 작성**

`web/app/events/page.tsx`:
```tsx
'use client';

import { useState } from 'react';
import EventCard from './components/EventCard';
import { useEvents } from '@/hooks/useEvents';

const SOURCES = [
  { key: undefined,       label: '전체' },
  { key: 'airbusan',     label: '에어부산' },
  { key: 'tway',         label: '티웨이항공' },
  { key: 'jejuair',      label: '제주항공' },
  { key: 'koreanair',    label: '대한항공' },
  { key: 'flightdeal',   label: 'flightdeal.kr' },
];

export default function EventsPage() {
  const [activeSource, setActiveSource] = useState<string | undefined>(undefined);
  const { data: events = [], isLoading } = useEvents(activeSource);

  return (
    <div className="mx-auto max-w-3xl px-4 py-6 space-y-6">
      <div>
        <h1 className="text-xl font-bold">항공사 특가 이벤트</h1>
        <p className="text-sm text-gray-500 mt-1">
          항공사 공식 이벤트 페이지에서 수집한 특가 정보입니다.
        </p>
      </div>

      {/* 필터 탭 */}
      <div className="flex flex-wrap gap-2">
        {SOURCES.map(({ key, label }) => (
          <button
            key={key ?? 'all'}
            onClick={() => setActiveSource(key)}
            className={`text-sm px-3 py-1 rounded-full border transition-colors ${
              activeSource === key
                ? 'bg-gray-900 text-white border-gray-900 dark:bg-white dark:text-gray-900'
                : 'border-gray-300 text-gray-600 hover:border-gray-500'
            }`}
          >
            {label}
          </button>
        ))}
      </div>

      {/* 이벤트 목록 */}
      {isLoading ? (
        <p className="text-sm text-gray-500">불러오는 중…</p>
      ) : events.length === 0 ? (
        <p className="text-sm text-gray-500">
          이벤트가 없습니다. Worker가 6시간마다 수집합니다.
        </p>
      ) : (
        <div className="grid gap-4">
          {events.map((event) => (
            <EventCard key={event.id} event={event} />
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 5: Nav에 이벤트 메뉴 추가**

`web/components/layout/Nav.tsx`를 열어 기존 링크 목록에 이벤트 메뉴 추가:
```tsx
<Link href="/events" className="...">이벤트</Link>
```
기존 Nav의 스타일 패턴을 따른다.

- [ ] **Step 6: 타입체크**

```bash
cd web && npm run typecheck
```

Expected: 에러 없음

- [ ] **Step 7: 커밋**

```bash
git add web/lib/types.ts web/hooks/useEvents.ts web/app/events/ \
        web/components/layout/Nav.tsx
git commit -m "feat: D — 이벤트 페이지 + Nav 메뉴 추가"
```

---

### Task 7: C — Stats 엔드포인트 + PriceHistoryChart

**Files:**
- Modify: `backend/app/schemas/watch.py` (WatchStats, MonthlyMin 추가)
- Modify: `backend/app/api/routes/watches.py` (GET /watches/{id}/stats)
- Create: `backend/tests/test_stats_endpoint.py`
- Modify: `web/lib/types.ts` (WatchStats 타입 추가)
- Modify: `web/hooks/useWatches.ts` (useWatchStats 추가)
- Create: `web/components/watch-detail/PriceHistoryChart.tsx`
- Modify: `web/app/watches/[id]/page.tsx` (PriceHistoryChart 삽입)

**Interfaces:**
- Produces: `GET /api/watches/{id}/stats → WatchStats`

- [ ] **Step 1: WatchStats 스키마 추가**

`backend/app/schemas/watch.py` 하단에 추가:
```python
class MonthlyMin(BaseModel):
    month: str    # "2026-07"
    min_krw: int


class WatchStats(BaseModel):
    monthly_min: list[MonthlyMin]
    overall_min: int | None
    overall_min_month: str | None
    data_months: int
```

- [ ] **Step 2: 실패하는 stats 테스트 작성**

`backend/tests/test_stats_endpoint.py`:
```python
"""GET /api/watches/{id}/stats 통합 테스트."""

import pytest
from httpx import AsyncClient
from datetime import datetime, timezone

from app.models.watch import Watch
from app.models.price import PriceSnapshot


@pytest.mark.asyncio
async def test_stats_returns_empty_for_no_snapshots(client: AsyncClient, async_session, auth_headers):
    # watch 생성
    watch = Watch(
        kind="flight",
        title="Stats Test",
        params={"kind": "flight", "origin": "ICN", "destination": "NRT",
                "depart_from": "2026-10-01", "depart_to": "2026-10-31"},
        rules=[],
        interval_min=360,
        status="active",
        next_run_at=datetime.now(tz=timezone.utc),
    )
    async_session.add(watch)
    await async_session.commit()

    resp = await client.get(f"/api/watches/{watch.id}/stats", headers=auth_headers)
    assert resp.status_code == 200
    data = resp.json()
    assert data["monthly_min"] == []
    assert data["overall_min"] is None
    assert data["data_months"] == 0


@pytest.mark.asyncio
async def test_stats_monthly_aggregation(client: AsyncClient, async_session, auth_headers):
    from sqlalchemy import text

    watch = Watch(
        kind="flight",
        title="Stats Monthly",
        params={"kind": "flight", "origin": "GMP", "destination": "CJU",
                "depart_from": "2026-09-01", "depart_to": "2026-09-30"},
        rules=[],
        interval_min=360,
        status="active",
        next_run_at=datetime.now(tz=timezone.utc),
    )
    async_session.add(watch)
    await async_session.commit()

    # 2026-08 스냅샷 2개: 300000, 350000 → 월 최저 300000
    for price in [300_000, 350_000]:
        snap = PriceSnapshot(
            watch_id=watch.id,
            min_price_krw=price,
        )
        async_session.add(snap)
    await async_session.commit()

    # captured_at을 2026-08로 강제 업데이트 (server_default가 now()라 직접 set 필요)
    await async_session.execute(
        text("UPDATE price_snapshots SET captured_at = '2026-08-15 00:00:00+00' WHERE watch_id = :wid"),
        {"wid": str(watch.id)},
    )
    await async_session.commit()

    resp = await client.get(f"/api/watches/{watch.id}/stats", headers=auth_headers)
    assert resp.status_code == 200
    data = resp.json()
    assert len(data["monthly_min"]) == 1
    assert data["monthly_min"][0]["month"] == "2026-08"
    assert data["monthly_min"][0]["min_krw"] == 300_000
    assert data["overall_min"] == 300_000
    assert data["data_months"] == 1
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
cd backend && uv run pytest tests/test_stats_endpoint.py -v
```

Expected: FAIL (엔드포인트 없음)

- [ ] **Step 4: stats 엔드포인트 구현**

`backend/app/api/routes/watches.py`에 import 추가:
```python
from app.schemas.watch import ..., MonthlyMin, WatchStats
```

그리고 엔드포인트 추가 (get_runs 뒤에):
```python
@router.get("/{watch_id}/stats", response_model=WatchStats, dependencies=[_auth])
async def get_stats(
    watch_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
):
    watch = await db.get(Watch, watch_id)
    if watch is None:
        raise HTTPException(status_code=404, detail="watch not found")

    result = await db.execute(
        text("""
            SELECT
                to_char(date_trunc('month', captured_at), 'YYYY-MM') AS month,
                MIN(min_price_krw) AS min_krw
            FROM price_snapshots
            WHERE watch_id = :watch_id
            GROUP BY 1
            ORDER BY 1
        """),
        {"watch_id": str(watch_id)},
    )
    rows = result.fetchall()

    monthly = [MonthlyMin(month=r[0], min_krw=r[1]) for r in rows]
    if monthly:
        best = min(monthly, key=lambda x: x.min_krw)
        overall_min = best.min_krw
        overall_min_month = best.month
    else:
        overall_min = None
        overall_min_month = None

    return WatchStats(
        monthly_min=monthly,
        overall_min=overall_min,
        overall_min_month=overall_min_month,
        data_months=len(monthly),
    )
```

`text` import가 이미 있는지 확인. 없으면 `from sqlalchemy import ..., text` 에 추가.

- [ ] **Step 5: 테스트 통과 확인**

```bash
cd backend && uv run pytest tests/test_stats_endpoint.py -v
cd backend && uv run pytest -q
```

Expected: 모든 테스트 PASS

- [ ] **Step 6: 커밋 (백엔드)**

```bash
git add backend/app/schemas/watch.py backend/app/api/routes/watches.py \
        backend/tests/test_stats_endpoint.py
git commit -m "feat: C — GET /api/watches/{id}/stats 월별 최저가 집계 엔드포인트"
```

- [ ] **Step 7: 프론트 types.ts에 WatchStats 추가**

`web/lib/types.ts` 하단에 추가:
```typescript
export interface MonthlyMin {
  month: string;   // "2026-07"
  min_krw: number;
}

export interface WatchStats {
  monthly_min: MonthlyMin[];
  overall_min: number | null;
  overall_min_month: string | null;
  data_months: number;
}
```

- [ ] **Step 8: useWatchStats 훅 추가**

`web/hooks/useWatches.ts` 하단에 추가:
```typescript
import type { ..., WatchStats } from '@/lib/types';

export function useWatchStats(id: string) {
  return useQuery<WatchStats>({
    queryKey: ['watches', id, 'stats'],
    queryFn: () => apiFetch<WatchStats>(`/api/watches/${id}/stats`),
    staleTime: 10 * 60_000,
  });
}
```

- [ ] **Step 9: PriceHistoryChart 컴포넌트 작성**

`web/components/watch-detail/PriceHistoryChart.tsx`:
```tsx
'use client';

import { formatKRW } from '@/lib/format';
import type { WatchStats } from '@/lib/types';

interface Props {
  stats: WatchStats;
}

export default function PriceHistoryChart({ stats }: Props) {
  if (stats.data_months < 1) {
    return (
      <p className="text-sm text-gray-500">
        최저가 패턴 분석 중 — 데이터가 더 쌓이면 표시됩니다.
      </p>
    );
  }

  const maxPrice = Math.max(...stats.monthly_min.map((m) => m.min_krw));
  const bestMonth = stats.overall_min_month;

  return (
    <div className="space-y-3">
      {stats.overall_min && stats.overall_min_month && (
        <p className="text-sm text-gray-700">
          <span className="font-semibold">
            {stats.overall_min_month.slice(5)}월
          </span>
          이 역대 가장 저렴했어요 ({formatKRW(stats.overall_min)})
        </p>
      )}
      <div className="flex items-end gap-1 h-24">
        {stats.monthly_min.map(({ month, min_krw }) => {
          const heightPct = maxPrice > 0 ? (min_krw / maxPrice) * 100 : 100;
          const isBest = month === bestMonth;
          return (
            <div
              key={month}
              className="flex-1 flex flex-col items-center justify-end gap-1"
              title={`${month}: ${formatKRW(min_krw)}`}
            >
              <div
                className={`w-full rounded-t ${
                  isBest ? 'bg-blue-500' : 'bg-gray-300 dark:bg-gray-600'
                }`}
                style={{ height: `${heightPct}%` }}
              />
              <span className="text-xs text-gray-500">{month.slice(5)}월</span>
            </div>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 10: 감시 상세 페이지에 PriceHistoryChart 추가**

`web/app/watches/[id]/page.tsx` 수정:

import 추가:
```tsx
import PriceHistoryChart from '@/components/watch-detail/PriceHistoryChart';
import { useWatchStats } from '@/hooks/useWatches';
```

훅 호출 추가 (기존 훅들 아래에):
```tsx
const { data: stats } = useWatchStats(id);
```

PriceChart 섹션 아래에 새 섹션 추가:
```tsx
{/* 월별 최저가 히스토리 */}
{stats && (
  <section>
    <h2 className="text-sm font-semibold mb-3">월별 최저가 패턴</h2>
    <PriceHistoryChart stats={stats} />
  </section>
)}
```

- [ ] **Step 11: 타입체크**

```bash
cd web && npm run typecheck
```

Expected: 에러 없음

- [ ] **Step 12: 커밋 (프론트)**

```bash
git add web/lib/types.ts web/hooks/useWatches.ts \
        web/components/watch-detail/PriceHistoryChart.tsx \
        web/app/watches/\[id\]/page.tsx
git commit -m "feat: C — 월별 최저가 차트 (PriceHistoryChart + useWatchStats)"
```

---

## 자체 검토 결과

**플레이스홀더 스캔:** 없음 ✅  
**타입 일관성:**
- `AirlineEventData` → `AirlineEvent` (모델) → `EventOut` (스키마) 경로 일치 ✅
- `deep_links: dict[str, str] | None` 백엔드/프론트 `Record<string, string> | null` 일치 ✅
- `MonthlyMin.month: str` ↔ `month: string` 일치 ✅

**스펙 커버리지:**
- B 딥링크 (백엔드 + 슬랙 + 프론트) → Task 1, 2 ✅
- C 가격 통계 (엔드포인트 + 차트) → Task 7 ✅
- D DB 모델 → Task 3 ✅
- D 파서 5개 → Task 4 ✅
- D Worker + API → Task 5 ✅
- D 프론트 이벤트 페이지 → Task 6 ✅

**Global Constraints 확인:**
- crawl_enabled 게이트 → Task 4 (policy.py) + Task 5 (event_tick 가드) ✅
- 봇 우회 없음 — honest User-Agent, rate limit 2초 ✅
- DB UTC timestamptz — `created_at_col()` 사용 ✅
- 가격 int 저장 ✅
